'use strict';

var ViewCtrl = angularApp.controller('RuntimeController', function ($rootScope, $scope, FormService, $routeParams) {

	// set Page Title variable when this controller is active
	$rootScope.pageTitle = 'Template Runtime';
});
